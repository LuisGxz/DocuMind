# DocuMind — Estado de fases

> Leer este archivo al iniciar sesión de trabajo. Detalle de cada fase en REQUIREMENTS.md.
> Vara de calidad: **FinPulse** (NO NEGOCIABLE — ver PORTFOLIO_PROJECTS.md §2 "Quality gate"). El proyecto N ≥ N-1 en pulido.

| Fase | Alcance | Estado |
|------|---------|--------|
| F1 | Fundaciones backend (NestJS, Prisma + Neon/pgvector + migración, docker Postgres, config, health, embeddings ONNX, pipeline de ingesta, seed corpus, tests dominio) | ✅ |
| F2 | Auth (JWT + refresh + lockout + RBAC Owner/Viewer), workspaces/membresía, usuarios+workspace demo seed | ✅ |
| F3 | Documentos API (biblioteca, detalle, upload + procesamiento async con estado/progreso, resumen, delete/reprocesar Owner) | ✅ |
| F4 | RAG chat (retrieval pgvector, AnswerGenerator extractivo + Claude listo, citas, SSE streaming, conversaciones, sugeridas) | ✅ |
| F5 | Frontend base (Angular 20 scaffold, design system DocuMind, i18n EN/ES, auth UI, shell, interceptores, ApiService) | ✅ |
| F6 | Frontend features (biblioteca, upload+progreso, split-view visor+chat streaming con citas, conversaciones, resumen, demo guiada completa) | ✅ |
| F7 | Pulido y entrega (/about bilingüe, TECHNICAL, README, CI, deploy Neon+Azure+Pages, E2E prod, card portfolio) | ✅ |

## Log
- 2026-06-17 · **F7 ✅** Entrega. **Demo full en vivo**: Pages `luisgxz.github.io/DocuMind` + API en Azure `documind-api-luisgxz.azurewebsites.net` (F1 Linux, rg `documind-rg`) + Neon Postgres/pgvector (`ep-still-breeze…us-east-1`, esquema+seed ya cargados: 2 users, 1 workspace, 6 docs, 16 chunks con embeddings) + card en el portfolio. `/health` → `{status:ok, db:up, llm:extractive}`. **E2E en prod verificado** (`frontend/e2e-prod.mjs`, Playwright contra Pages+Azure reales): Owner login→biblioteca (6 cards, botón Subir)→abre MSA→pregunta terminación→**streaming SSE con 2 chips de cita + 2 pasajes resaltados** ("Section 9 — Termination" en el visor); Viewer RBAC (pill "Lector", 0 botones subir, 0 acciones owner, **sí pregunta** con cita); **0 errores de consola**. Screenshots prod en `docs/screenshots/prod-*.png`. **El gran blocker (deploy Node en Azure F1)**: el OneDeploy del árbol de ~1GB (`onnxruntime-node` solo = 210MB) **expiraba con 504** extrayendo en el disco F1; los reintentos de 46min **agotaron la cuota CPU diaria de F1** (`state: QuotaExceeded`, reset a medianoche UTC). **Solución**: (1) **run-from-package** (`WEBSITE_RUN_FROM_PACKAGE=1`) — el zip se **monta**, no se extrae → sin 504; (2) **bundle del modelo** ONNX en `./models` con `ALLOW_REMOTE_MODELS=false` → funciona en el mount read-only y sin descarga de HuggingFace en cold-start (`embedding.service.ts` apunta `env.cacheDir` ahí; `scripts/fetch-model.mjs` lo baja en CI); (3) **podar binarios onnxruntime no-linux** (darwin/win32, −158MB); (4) deploy en CI vía **SP + `az webapp deploy --type zip`** (honra run-from-package; antes era `azure/webapps-deploy` con publish-profile). Para resetear la cuota agotada: **recrear app+plan F1** (instancia nueva = contador en cero) y re-aplicar los 13 app settings + startup `node dist/main.js`. **Trampas F7**: (a) MSYS convierte `--scopes /subscriptions/...` a `C:/Program Files/Git/...` → `MSYS_NO_PATHCONV=1` al crear el SP; (b) 404 de consola en deep-link `/DocuMind/login` = **fallback SPA de Pages benigno** (404.html→index.html), se evita entrando por la raíz `/DocuMind/`; (c) el resaltado de cita necesita doc+pregunta coherentes (abrir el MSA para la pregunta de terminación, no el employment).
- 2026-06-16 · **F6 ✅** Frontend features. **Split-view documento+chat** (`/d/:id`, `document.component`): top-bar (back, filename, badge de estado, acciones Owner reprocess/delete con confirm + poll hasta Indexed), resumen colapsable (`<details>`, bilingüe vía `lang.pick`), grid 2-col visor|chat (tabs Documento/Chat en ≤820px). **`doc-viewer`**: texto paginado reconstruido de `/content`, resaltado de citas (`highlight.util` parte el texto en segmentos cited/hot tolerando el `…` de quotes recortadas), **auto-scroll** a la cita activa (effect + queueMicrotask + scrollIntoView). **`chat-panel`** (el núcleo): streaming **SSE vía fetch+ReadableStream** (`ChatService` parsea frames `data:` con retry de refresh en 401), efecto typing (caret), mensajes user/assistant, **fuentes como chips** (hover/focus→emite chunkId→resalta en el visor), preguntas **sugeridas** (chips), **conversaciones** (dropdown: listar/cambiar/eliminar, nuevo chat; continúa con `conversationId` y rehidrata citas del último assistant), estados empty/thinking/error. **Upload** (`upload-dialog`, solo Owner): drag-drop + file picker, POST multipart, **polling de progreso** (Queued→Processing con barra %→Indexed) y CTA abrir/subir-otro; nota "procesado localmente". **Demo guiada (RF-09)**: `tour.component` (coach-marks de primer ingreso, 3 pasos con dots, persistido en `localStorage`, replay desde el botón `?` del header) + panel **"Ve el RBAC en vivo"** role-aware en la biblioteca (escenario cross-rol primero + qué puede/no el rol actual). **Infra**: `WorkspaceStore` (signal compartido del workspace+rol, refrescado tras upload/delete), `DemoService`. Biblioteca: cards ahora enlazan a `/d/:id`, botón Subir (Owner). **Verificación**: build limpio; **E2E Playwright completo** — login Owner→biblioteca→abrir doc→preguntar→**streaming con 2 chips de cita + 1 pasaje resaltado** en el visor; **RBAC Viewer** (badge Lector, 0 botones subir, 0 acciones owner, pero **sí pregunta** con citas); **upload real** (md→indexado→abrir→pregunta citando la cláusula); tour de primer ingreso. **Screenshots a 390/768/1280** (library/doc/answer/tour/upload), **0 errores de consola** en todos. **Trampas**: (1) una variable de template `#fileInput` ensombrece al signal `fileInput()` del componente → "not callable"; usar la var de template directo (`fileEl.click()`) o renombrar. (2) En ≤820px el `dm-chat-panel` está `display:none` por defecto (tab Documento) → en E2E esperar `.split` (attached), no `dm-chat-panel` visible. (3) clase `.doc` ya colisionaba (resuelta en F5, mantener `.dcard`). **Pendiente F7**: /about bilingüe, TECHNICAL/README, CI, deploy Neon+Azure+Pages, E2E prod, card del portfolio.
- 2026-06-16 · **F5 ✅** Frontend base (Angular 20). **Scaffold** reusando la config exacta de ShopForge (Angular 20.3, `@angular/build`, standalone + signals, SCSS, sin Tailwind). **Design system** `styles.scss` con tokens del mockup (canvas #F7F8FA, escala ink, aiblue/aiviolet + gradiente, cite #FFF3C4, Inter + Source Serif 4) y utilidades (.btn/.input/.card/.badge/.role-pill/.alert/.caret/.skeleton/.ai-grad + `prefers-reduced-motion`). **Core**: `config` (API base inyectable `window.DOCUMIND_API_BASE`), `models` (todos los DTOs del backend), `ApiService` (todos los endpoints: workspaces, documents CRUD+content+status+upload+reprocess, suggested-questions, conversations CRUD), `AuthService` (signals access+refresh, localStorage), `auth.interceptor` (bearer + auto-refresh 1x en 401), `guards` (authGuard/guestGuard), `api-error` (ProblemDetails por campo + fallback honesto en 0/429/5xx), `i18n` (AppCopy tipado EN/ES + mapas de display status/role/fileKind), `LanguageService` (signal + pick bilingüe). **Shared**: `dm-icon` (set inline-SVG Lucide-style), `dm-brand` (sparkles sobre gradiente), `dm-lang-toggle`. **Layout** `dm-shell` (header sticky con brand, nav, **badge de rol persistente** del workspace, nombre + sign-out, lang toggle; footer; router-outlet). **Auth UI** login (split hero gradiente + form, **cuentas demo Owner/Viewer con role pills y auto-login**, returnUrl) y register (nota de rol Viewer), ambos con loading/errores/validación. **Library** (read-only en F5): header de workspace bilingüe + contadores + estados loading(skeletons)/empty/error(retry); grid de cards con icono por fileKind, badge de estado, nº conversaciones (destaca con borde aiblue), uploadedBy. **app.config** con `provideAppInitializer(tryRefresh)` (sesión restaurada al boot). **Verificación**: build limpio; **E2E real con Playwright** (login owner vía botón demo → biblioteca con 6 cards + badge de rol) y **screenshots a 3 breakpoints** (390/768/1280) de login/register/library, **0 errores de consola**. **Trampas**: (1) colisión de clase — nombré la card `.doc` y chocó con el helper global `.doc{font-family:Source Serif 4}` → los filenames salían serif; renombrada a `.dcard`. (2) Otro dev-server ajeno ocupaba el :4200 (proyecto tickets-system) → Playwright golpeaba esa app; usar puerto libre :4173 y añadirlo a `CORS_ORIGINS` del backend. (3) `:global()` no existe en Angular → `::ng-deep` para el lang-toggle sobre el gradiente. (4) lang toggle vive en el hero (oculto en mobile) → añadido `.panel-top` solo-mobile en auth. **Pendiente F6**: upload+progreso, split-view visor+chat streaming con citas (fetch+ReadableStream SSE), conversaciones, resumen en detalle, demo guiada; F7 entrega.
- 2026-06-16 · **F4 ✅** RAG chat. **Capa de generación** (`generation/`, módulo propio sin deps de app para no ciclar con ingestion ni rag): interfaz `AnswerGenerator` con dos implementaciones elegidas por `AppConfig.claudeEnabled` en un factory (`ANSWER_GENERATOR` symbol). **ExtractiveAnswerGenerator** (default, sin key, determinista): `compose()` puro y testeado filtra por `RELEVANCE_FLOOR` (cos≥0.25), extrae keywords (stopwords + stem-prefix overlap), selecciona la mejor frase por chunk (overlap·10 + score), arma respuesta con citas inline `(§ p. N)` y `Citation{chunkId,page,quote}` reales; honesto cuando no hay contexto (no alucina). `generate()` streamea palabra-a-palabra (typing) y emite `sources`. `summarizeExtractive()` = TextRank-lite (frec. de palabras de contenido → top-4 frases en orden) → `summaryEs` null. **ClaudeAnswerGenerator** (real, dormido sin key): `@anthropic-ai/sdk` v0.104, `messages.stream` con `thinking:{type:'adaptive'}` (claude-opus-4-8, configurable `ANTHROPIC_MODEL`), citas parseadas de los marcadores `(§ p. N)` → mejor chunk de esa página; summary vía `output_config.format` json_schema EN+ES. **RetrievalService**: pgvector top-k SQL crudo `1 - (embedding <=> $1::vector) AS score ... ORDER BY embedding <=> $1::vector` (k=6). **RagService.chat()** = async-generator: valida `Indexed`→400 si no; resuelve/crea conversación; snapshot de history (6 turnos) **antes** de persistir el user msg; retrieve; streamea del generador acumulando `full`+citations; persiste assistant msg con citations (jsonb); `touch`; emite `meta→token*→sources→done{messageId}`. **suggestQuestions** document-aware (regex sobre el texto de los chunks → preguntas legales, fallback genérico). **SSE** vía `@Res()` manual (`res.flushHeaders`, `data: ...\n\n`), error post-headers → evento `error`; pre-headers lo maneja el filtro global. **Conversations** (módulo propio): privadas por (doc,user) — todo scoped por userId (404 si no), list/detail/create/rename/delete; `getOwned/addMessage/recentHistory/touch` para el RAG. **Resumen automático (RF-07)** wired en `IngestionService` tras indexar (best-effort try/catch, no falla la ingesta). **Tests**: 50 verdes (+14: compose con citas/multi-página/honesto-sin-contexto/honesto-sin-relevancia, generate streaming==compose, summarizeExtractive, text.util keywords/overlap-stem/split/clamp, citationsFromText marker→mejor-chunk/ignora-páginas-ausentes/dedup). **Smoke HTTP real** (modo extractivo, reseed limpio): los 6 docs con `hasSummary=true`; suggested-questions document-aware; chat SSE `meta→tokens→sources(3 citas chunkId+page reales)→done`; honestidad sin contexto (0 citas); Viewer puede preguntar (RF-02); continuación de conversación (mismo convId, 4 msgs persistidos con citations); aislamiento de propiedad (owner lee conv de viewer→404); rename→ok, delete→204, validación question vacía/>2000/conversationId-no-uuid→400 ProblemDetails por campo; sin token→401. **Trampas**: (1) `import type` para `AnswerGenerator`/`Response` en firmas decoradas (isolatedModules+emitDecoratorMetadata, recurrente). (2) Para regenerar resúmenes el seed debe re-ingestar — salta docs ya `Indexed`, así que hay que **recrear el volumen** (`docker compose down -v`) para un seed limpio con summaries. (3) curl en MSYS escribe `/tmp/x` pero node lo lee como `C:\tmp\x` → usar pipes/stdin, no archivos temporales compartidos. **Diferido a F7/PENDING**: probar Claude real con key; PDF binario real; persistir binario original.
- 2026-06-16 · **F3 ✅** Documentos API. **Biblioteca** `GET /workspaces/:slug/documents` (cualquier miembro) con fileKind derivado, estado, progreso, chunkCount, nº de conversaciones, quién subió. **Detalle** `GET /documents/:id` (+ summary/statusDetail). **Estado** `GET /documents/:id/status` (para polling del procesamiento). **Contenido** `GET /documents/:id/content` (reconstruye páginas desde los chunks para el visor — citas mapean al chunk exacto). **Upload** `POST /workspaces/:slug/documents` (solo **Owner**, `FileInterceptor` memoria, valida mime PDF/TXT/MD y tamaño máx; crea Document `Queued` y **encola ingesta async**; el cliente hace polling del estado). **Reprocess** `POST /documents/:id/reprocess` (Owner; re-embebe desde el texto de los chunks ya guardados — sin bytes originales, ver PENDING.md). **Delete** `DELETE /documents/:id` (Owner). RBAC vía nuevo `DocumentAccessGuard` (resuelve workspace desde el documento + `@RequireRole`) y `@CurrentDocument`. **Tests**: 36 verdes (+10: fileKindOf, validación de upload mime/tamaño/ausente, DocumentAccessGuard 401/404/403/role). **Smoke HTTP real**: biblioteca (6 docs), content (pág.5 = cláusula de terminación), viewer upload/delete→403, owner upload→Queued→Processing→Indexed (2 págs/2 chunks), reprocess→201 re-indexado, owner delete→204, mime inválido→400 con mensaje. **Trampa de test (no del código)**: curl de Windows no lee rutas MSYS `/tmp/...` (error 26) ni mata jobs `%1` entre llamadas de Bash → usar rutas locales y `taskkill` por PID del puerto. **Diferido a F4/PENDING**: resumen automático (necesita AnswerGenerator), persistir binario original (descarga + reprocess fiel), test de PDF real.
- 2026-06-16 · **F2 ✅** Auth + RBAC. **JWT** access (15 min, payload sub/email/name) + **refresh rotativo** (7 días, token aleatorio de 256 bits, se guarda solo el **sha256** → lookup directo; rotación marca el viejo `revokedAt`+`replacedByHash`; **detección de reuso** revoca toda la familia). **argon2** para contraseñas. **Lockout** (5 intentos → 15 min; reset al login correcto). **No-enumeración**: email desconocido y contraseña errónea devuelven el mismo `401 Invalid email or password.`. **RBAC Owner/Viewer**: `WorkspaceAccessGuard` resuelve la membresía desde `:slug`/`workspaceId` y aplica `@RequireRole(...)` (reusable para F3); `@CurrentMembership` inyecta el contexto. **Workspaces**: `GET /workspaces` (mis workspaces con rol+conteos), `GET /workspaces/:slug` (detalle, requiere membresía). **Rate limiting** (`@nestjs/throttler` global 120/min + `/auth` 10/min, `/auth/me` 60/min). Endpoints: `/auth/register|login|refresh|logout|me`. **Tests**: 26 verdes (parseDuration, AuthService con fake Prisma — dup→409, no-enumeración, lockout a los 5, reset, rotación, reuso→familia revocada, logout idempotente; WorkspaceAccessGuard — no-miembro/Viewer-en-Owner→403, Owner→ok, slug inexistente→404). **Smoke HTTP real** verificado: login owner/viewer, /me, workspaces con rol+conteos (6 docs/16 págs), rotación, reuso→401, validación con `errors` por campo (ProblemDetails), sin token→401. **Trampa**: `isolatedModules`+`emitDecoratorMetadata` exige `import type` para tipos en firmas decoradas; `JwtService.signAsync` exige `expiresIn` numérico (no `string`).
- 2026-06-15 · **F1 ✅** Fundaciones backend. **NestJS** (Node 24, TS estricto) + config tipada/validada (`AppConfig`, falla rápido si falta env). **Prisma 6** (downgrade desde 7 por el cambio de `prisma.config.ts`) + **Postgres/pgvector** (docker `pgvector/pgvector:pg17`, puerto 5434) — migración `init` con columna `vector(384)` e **índice HNSW** `vector_cosine_ops` inyectado a mano en el SQL. Modelo: User/RefreshToken/Workspace/Membership(Owner|Viewer)/Document(status+progreso)/DocumentChunk(embedding)/Conversation/ChatMessage. **EmbeddingService** ONNX local `Xenova/all-MiniLM-L6-v2` (384d, normalizado, singleton lazy). **Pipeline de ingesta** (`IngestionService`: extract→chunk→embed→store con estado/progreso; extractores PDF vía pdf-parse v2 per-page + texto/markdown con page-breaks `\f`; chunker con solape preservando página; insert de embeddings por **SQL crudo** `$n::vector`/`::uuid`). **Seed**: workspace "Legal & Contracts" + usuarios demo **owner@documind.dev/Owner1234!** y **viewer@documind.dev/Viewer1234!** + corpus realista (MSA, DPA, NDA, SaaS terms, IP assignment, employment) = **6 docs / 16 págs / 16 chunks**. `/health` (db + modo LLM). **Retrieval pgvector verificado** (la cláusula de terminación sale primera ante "end the contract early"). Tests: chunker (6) + cosine (4) + e2e `/health` (1). Build+lint+tests verdes. **Trampas resueltas**: Prisma 7 `url` deprecado→downgrade a 6; `tsx` no emite `emitDecoratorMetadata`→NestJS DI `undefined`, seed corre **compilado** (`nest build && node dist/...`); `.env` no autocargado en tsx/nest→`dotenv/config`; jest rompe el realm de `Float32Array` de onnxruntime→test del modelo movido al camino real (seed/retrieval).
- 2026-06-15 · Repo creado. REQUIREMENTS.md + arquitectura definidos. Decisiones: LLM extractivo determinista por defecto con adapter Claude listo (sin key); embeddings locales ONNX all-MiniLM (384d); pgvector en Neon; Prisma + SQL crudo para vectores; visor de texto extraído. Stack: Angular 20 + NestJS + Postgres/pgvector.

## Cómo correr (dev)
- DB: `docker compose up -d` (Postgres 17 + pgvector) o apuntar `DATABASE_URL` a Neon.
- Backend: `cd backend && npm install && npx prisma migrate dev && npm run seed && npm run start:dev` (http://localhost:3000).
- Demo (desde F2): se definirán cuentas Owner/Viewer en el seed.
- Modo LLM: sin `ANTHROPIC_API_KEY` → generador extractivo determinista. Con key → Claude real (claude-opus-4-8 / claude-haiku-4-5).
